int4 rows[2] = {{1, 2, 3, 4}, {5, 6, 7, 8}};

int main() {
    rows[0] = rows[0] + rows[1];
    rows[1][2] = rows[0][3] * 2;
    putint(rows[0][0] + rows[0][1] * 10 + rows[0][2] * 100
           + rows[0][3] * 1000 + rows[1][2] * 10000);
    putch(10);
    return 0;
}
