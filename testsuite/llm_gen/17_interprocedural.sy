// Corner: constant propagation across multiple functions (should NOT propagate)
int f(int x) { return x + 1; }
int g(int x) { return f(x) + 1; }

int main() {
    int a = g(10);
    putint(a);
    return a;
}
