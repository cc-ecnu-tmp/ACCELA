// Corner: zero division guard - optimizer must not fold div by zero
int main() {
    int x = getint();
    int y;
    if (x == 0) {
        y = 0;
    } else {
        y = 100 / x;
    }
    putint(y);
    return y;
}
