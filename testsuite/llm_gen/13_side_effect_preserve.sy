// Corner: dead code with side effects must NOT be removed
int g = 0;
int side() {
    g = g + 1;
    return g;
}
int main() {
    int a = side();
    int b = side();
    int c = side();
    putint(g);
    return g;
}
